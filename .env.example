# NabhaVerse root environment template
#
# Root-level runtime variables are not required for the current local setup.
# Use the app-specific files below:
#   - apps/web/.env.example
#   - apps/api/.env.example
#
# Optional local overrides can be added here when repository-wide settings are introduced.
